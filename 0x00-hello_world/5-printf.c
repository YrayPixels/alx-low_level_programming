#include <stdio.h>
/**
 * main - to print a string
 * Description: we are using printf to print
 * Return: 0
 */
int main(void)
{
printf("%s", "with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
